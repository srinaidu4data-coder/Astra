---
phase: 06-answer-formats
plan: 01
type: execute
wave: 1
depends_on: [05-01]
files_modified: [rag.py, gui.py]
autonomous: true

must_haves:
  truths:
    - "Bullet format produces exactly 2-3 concise key points"
    - "Conversational format produces natural, speakable text"
    - "Tone setting affects conversational output style"
    - "Both formats use RAG context from documents"
  artifacts:
    - path: "rag.py"
      provides: "Bullet point prompt and generation"
      contains: "BULLET_SYSTEM_PROMPT"
    - path: "rag.py"
      provides: "Script prompt with tone parameter"
      contains: "SCRIPT_SYSTEM_PROMPT"
    - path: "rag.py"
      provides: "Tone configuration"
      contains: "generate_script_response"
  key_links:
    - from: "generate_bullet_response"
      to: "bullet_box"
      via: "signal connection"
      pattern: "bullet_token"
---

<objective>
Implement bullet point and conversational script generation with configurable tone.

Purpose: Enable two distinct answer formats - quick bullet points for rapid reference and humanized conversational script for reading aloud.
Output: Modified rag.py with two new prompts and generation functions, updated gui.py to wire them to the dual-pane UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Current RAG implementation (patterns to follow):
@rag.py

# Current GUI with dual-pane layout:
@gui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bullet point prompt and generation function</name>
  <files>rag.py</files>
  <action>
Add bullet point format generation to rag.py:

1. Create BULLET_SYSTEM_PROMPT after STAR_SYSTEM_PROMPT:
   - Role: Generate 2-3 concise key points for interview answers
   - Format: Exactly 2-3 bullet points, each 1-2 sentences max
   - Content: Technical essentials only - t-codes, config paths, key terms
   - Style: Quick-reference, scannable, not speakable prose
   - Must use RAG context when relevant

2. Create generate_bullet_response() function:
   - Signature: `def generate_bullet_response(question: str, context_chunks: list[dict], job_context: str = "") -> Generator[str, None, None]`
   - Pattern: Follow existing generate_star_response() structure
   - Model: Use gpt-4o-mini for speed (bullets are simple)
   - Temperature: 0.3 (more focused output)
   - Context handling: Same logic as STAR response

3. Create ask_bullet() entry point:
   - Signature: `def ask_bullet(question: str, job_context: str = "") -> Generator[str, None, None]`
   - Pattern: Search context, then generate bullet response
  </action>
  <verify>python3 -c "from rag import ask_bullet, BULLET_SYSTEM_PROMPT; print('Bullet format OK')"</verify>
  <done>BULLET_SYSTEM_PROMPT and generate_bullet_response() added to rag.py</done>
</task>

<task type="auto">
  <name>Task 2: Create conversational script prompt with tone parameter</name>
  <files>rag.py</files>
  <action>
Add conversational script generation with configurable tone:

1. Create SCRIPT_SYSTEM_PROMPT after BULLET_SYSTEM_PROMPT:
   - Role: Generate humanized, speakable interview script
   - Format: Natural conversational flow, readable aloud verbatim
   - Style: Smooth transitions, no bullet points, complete sentences
   - Tone placeholder: {tone_instruction} for dynamic tone injection
   - Must use RAG context when relevant
   - Based on existing STAR_SYSTEM_PROMPT but focused on speakability

2. Create TONE_INSTRUCTIONS dict:
   ```python
   TONE_INSTRUCTIONS = {
       "professional": "Use formal but warm language. Sound composed and authoritative.",
       "casual": "Use relaxed, friendly language. Sound approachable and conversational.",
       "confident": "Use assertive, direct language. Sound self-assured and commanding."
   }
   ```

3. Create generate_script_response() function:
   - Signature: `def generate_script_response(question: str, context_chunks: list[dict], job_context: str = "", tone: str = "professional") -> Generator[str, None, None]`
   - Pattern: Follow existing generate_star_response() structure
   - Model: gpt-4o (needs quality for natural speech)
   - Temperature: 0.7 (creative for natural flow)
   - Inject tone instruction from TONE_INSTRUCTIONS dict
   - Default tone: "professional"

4. Create ask_script() entry point:
   - Signature: `def ask_script(question: str, job_context: str = "", tone: str = "professional") -> Generator[str, None, None]`
   - Pattern: Search context, then generate script response
  </action>
  <verify>python3 -c "from rag import ask_script, SCRIPT_SYSTEM_PROMPT, TONE_INSTRUCTIONS; print('Script format OK')"</verify>
  <done>SCRIPT_SYSTEM_PROMPT, TONE_INSTRUCTIONS, and generate_script_response() added to rag.py</done>
</task>

<task type="auto">
  <name>Task 3: Wire new formats to dual-pane UI</name>
  <files>gui.py</files>
  <action>
Update gui.py to use the new format generators:

1. Add tone setting to AstraWindow:
   - Add `self.current_tone = "professional"` in __init__
   - (Tone UI selector will be added in future enhancement, hardcode for now)

2. Update _do_answer_generation() method to call ask_bullet():
   - Import ask_bullet from rag
   - Replace current ask() call with ask_bullet() for bullet_box
   - Stream tokens via signals.bullet_token (existing signal from Phase 5)

3. The script generation will be added in Phase 7 (parallel execution)
   - For now, script_box shows placeholder text
   - Add comment: "# TODO Phase 7: Add parallel script generation"

4. Update question display:
   - In the answer generation flow, emit signals.question_update with the question text
   - This updates the question_display label at top of answer area

Note: Phase 7 will add parallel execution of both formats. This phase only adds the generation functions and wires bullet format to existing flow.
  </action>
  <verify>python3 -c "from gui import AstraWindow; print('GUI import OK')"</verify>
  <done>Bullet format wired to bullet_box, question displayed at top</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python3 -c "from rag import ask_bullet, ask_script"` - imports without error
- [ ] `python3 -c "from rag import BULLET_SYSTEM_PROMPT, SCRIPT_SYSTEM_PROMPT, TONE_INSTRUCTIONS"` - prompts exist
- [ ] `python3 -c "from gui import AstraWindow"` - GUI imports without error
- [ ] Verify BULLET_SYSTEM_PROMPT produces 2-3 bullet format (check prompt text)
- [ ] Verify SCRIPT_SYSTEM_PROMPT includes tone placeholder
- [ ] Verify TONE_INSTRUCTIONS has professional, casual, confident keys
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Bullet format prompt produces concise 2-3 key points
- Script format prompt produces speakable text with tone control
- Bullet generation wired to bullet_box in GUI
- Question displayed at top of answer area when generating
</success_criteria>

<output>
After completion, create `.planning/phases/06-answer-formats/06-01-SUMMARY.md`
</output>
