---
phase: 01-startup-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [gui.py, main.py]
autonomous: true

must_haves:
  truths:
    - "App launches to startup screen with two buttons visible"
    - "Clicking 'Ingest Documents' triggers document ingestion"
    - "Clicking 'Start Session' navigates to session screen"
    - "Session screen remains fully functional after navigation"
  artifacts:
    - path: "gui.py"
      provides: "StartupScreen class with two buttons"
      contains: "class StartupScreen"
    - path: "gui.py"
      provides: "Navigation between startup and session screens"
      contains: "show_session_screen"
  key_links:
    - from: "StartupScreen.ingest_btn"
      to: "ingest.ingest_folder"
      via: "clicked signal → _on_ingest_clicked"
      pattern: "ingest_folder"
    - from: "StartupScreen.start_btn"
      to: "AstraWindow"
      via: "clicked signal → show_session_screen"
      pattern: "show_session_screen"
---

<objective>
Create a startup screen with "Ingest Documents" and "Start Session" buttons, replacing direct session launch.

Purpose: Give users clear entry point with two options instead of requiring CLI for document ingestion.
Output: Modified gui.py with StartupScreen class and navigation between screens.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source files:
@gui.py
@main.py
@ingest.py

Relevant patterns from codebase:
- PyQt6 GUI with QMainWindow
- Thread-safe updates via SignalBridge
- Stylesheet formatting with multi-line CSS strings
- Signal connections: clicked.connect(self._on_handler)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StartupScreen widget</name>
  <files>gui.py</files>
  <action>
Create a StartupScreen class (QWidget) in gui.py with:

1. Two prominent buttons arranged vertically:
   - "Ingest Documents" button (triggers document ingestion from documents/ folder)
   - "Start Session" button (navigates to session screen)

2. Title label "Astra Interview Copilot" at top

3. Brief instructions text explaining:
   - "Ingest Documents" scans the documents/ folder
   - "Start Session" starts the interview copilot

4. Styling consistent with existing AstraWindow:
   - White background (#ffffff)
   - Blue primary button (#4a90d9)
   - Green secondary button (#28a745)
   - Same font family and styling patterns

5. Add signals to StartupScreen:
   - ingest_requested = pyqtSignal()
   - start_session_requested = pyqtSignal()

6. Connect button clicks to emit these signals

Do NOT modify AstraWindow yet - just create the new widget.
  </action>
  <verify>Class StartupScreen exists in gui.py with buttons and signals</verify>
  <done>StartupScreen widget created with two buttons and appropriate signals</done>
</task>

<task type="auto">
  <name>Task 2: Wire navigation and update entry point</name>
  <files>gui.py, main.py</files>
  <action>
1. Create AstraApp class in gui.py that manages screen transitions:
   - Holds reference to StartupScreen and AstraWindow
   - On ingest_requested: run ingestion in background thread, show progress/status
   - On start_session_requested: hide startup, show session window
   - Allow returning to startup from session (optional - add menu item or close behavior)

2. For document ingestion:
   - Run ingest_folder("./documents/") in background thread
   - Show simple status dialog or update StartupScreen with progress
   - After completion, show success/error message
   - Enable "Start Session" button after successful ingestion (or always enabled)

3. Update main.py:
   - Remove the check_documents_exist() gate that blocks GUI launch
   - Launch AstraApp instead of directly launching AstraWindow
   - Keep --ingest CLI flag working for CLI-based ingestion

4. Important: AstraWindow should remain unchanged internally - only change how it's instantiated/shown.

Implementation pattern:
```python
class AstraApp:
    def __init__(self):
        self.startup_screen = StartupScreen()
        self.session_window = None  # Lazy create

        self.startup_screen.ingest_requested.connect(self._on_ingest)
        self.startup_screen.start_session_requested.connect(self._on_start_session)

    def show(self):
        self.startup_screen.show()

    def _on_start_session(self):
        if self.session_window is None:
            self.session_window = AstraWindow()
        self.startup_screen.hide()
        self.session_window.show()
```
  </action>
  <verify>
    - python main.py shows startup screen first
    - "Start Session" button shows AstraWindow
    - python main.py --ingest ./documents/ still works from CLI
  </verify>
  <done>
    - App launches to startup screen
    - Both buttons work correctly
    - Session screen fully functional
    - CLI ingestion still works
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python main.py` launches startup screen (not session directly)
- [ ] "Ingest Documents" button triggers ingestion of documents/ folder
- [ ] "Start Session" button navigates to session screen
- [ ] Session screen (AstraWindow) works normally after navigation
- [ ] `python main.py --ingest ./path/` CLI still works
- [ ] No Python errors or warnings
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- StartupScreen class exists with proper styling
- Navigation between screens works
- Existing AstraWindow functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-startup-screen/01-01-SUMMARY.md`
</output>
