---
phase: 03-resizable-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [gui.py]
autonomous: true

must_haves:
  truths:
    - "Window can be resized by dragging edges and corners"
    - "UI elements scale appropriately when window resized"
    - "Minimum window size prevents unusable layouts"
    - "Horizontal layout shows Question left, Answer right on wide screens"
    - "Vertical layout maintained for narrow windows"
  artifacts:
    - path: "gui.py"
      provides: "Resizable window with responsive layout"
      contains: "setMinimumSize"
  key_links:
    - from: "AstraWindow._init_ui"
      to: "QSplitter or responsive layout"
      via: "layout management"
      pattern: "QSplitter|horizontalLayout"
---

<objective>
Make window resizable with minimum size constraints and horizontal layout option.

Purpose: Users with wider screens can see Question and Answer side-by-side; all users can resize to their preference.
Output: Modified gui.py with resizable windows and responsive layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-startup-screen/01-01-SUMMARY.md

Source files:
@gui.py

Current layout constraints:
- StartupScreen: setFixedSize(400, 350) at line 86
- AstraWindow: setFixedSize(500, 750) at line 254
- Both use QVBoxLayout (vertical only)

PyQt6 patterns for responsive layout:
- setMinimumSize() instead of setFixedSize() for resizability
- QSplitter for user-adjustable split between panels
- setSizePolicy() for widget stretch behavior
- resizeEvent() for layout switching based on width
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make StartupScreen resizable with minimum size</name>
  <files>gui.py</files>
  <action>
Modify StartupScreen._init_ui():

1. Replace `setFixedSize(400, 350)` with `setMinimumSize(350, 300)`
2. Add `self.resize(400, 350)` to set initial size (but allow growth)
3. Add stretch factor to layout so content centers vertically when taller
4. Ensure buttons maintain reasonable size (setMinimumHeight already set)

The startup screen is simple enough that no horizontal layout variant is needed - just resizable.
  </action>
  <verify>python -c "from gui import StartupScreen; from PyQt6.QtWidgets import QApplication; app = QApplication([]); s = StartupScreen(); print('minimumSize:', s.minimumSize().width(), s.minimumSize().height())"</verify>
  <done>StartupScreen uses setMinimumSize instead of setFixedSize, window is resizable</done>
</task>

<task type="auto">
  <name>Task 2: Make AstraWindow resizable with horizontal layout option</name>
  <files>gui.py</files>
  <action>
Modify AstraWindow class:

1. In _init_ui(), replace `setFixedSize(500, 750)` with:
   - `self.setMinimumSize(450, 600)` - minimum usable size
   - `self.resize(600, 750)` - comfortable default

2. Create a content area using QSplitter for Question/Answer sections:
   - Create QSplitter with Qt.Orientation.Horizontal orientation (can be toggled)
   - Left/top panel: Question-related widgets (transcription_box, last_heard section)
   - Right/bottom panel: Answer section (answer_box)
   - Set initial splitter sizes for 40/60 split

3. Add layout mode tracking:
   - Add `self.horizontal_layout = False` state
   - Add method `_update_layout_orientation()` that:
     - Sets splitter orientation based on self.horizontal_layout
     - Adjusts text box heights (shorter in horizontal, taller in vertical)

4. Override `resizeEvent(self, event)`:
   - If width >= 900, suggest horizontal layout is available
   - Auto-switch is optional; manual toggle is sufficient for v1

5. Add layout toggle button (small, in title area or status bar):
   - Icon: "⇔" for horizontal, "⇕" for vertical
   - Placed near the title or in audio source row
   - Clicking calls _toggle_layout() which:
     - Flips self.horizontal_layout
     - Calls _update_layout_orientation()
     - Updates button icon

Implementation notes:
- QSplitter allows user to drag divider between Question/Answer
- Keep all existing widgets in their functional groups
- Minimum panel sizes prevent collapsing below usable size
  </action>
  <verify>
    - python -c "from gui import AstraWindow; from PyQt6.QtWidgets import QApplication; app = QApplication([]); w = AstraWindow(); print('minimumSize:', w.minimumSize().width(), w.minimumSize().height())"
    - Manual: Run `python main.py`, click "Start Session", verify window is resizable
  </verify>
  <done>
    - AstraWindow is resizable with minimum 450x600
    - QSplitter allows adjusting Question/Answer panel sizes
    - Layout toggle button switches between horizontal and vertical modes
    - Horizontal mode places Question left, Answer right
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python main.py` launches without errors
- [ ] Startup screen is resizable (not fixed size)
- [ ] Session window is resizable (not fixed size)
- [ ] Session window has minimum size enforced
- [ ] Splitter divider can be dragged to resize Question/Answer panels
- [ ] Layout toggle button switches between vertical and horizontal
- [ ] In horizontal mode: Question left, Answer right
- [ ] In vertical mode: Question top, Answer bottom
- [ ] No Python errors or warnings
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Windows are resizable with enforced minimum sizes
- Horizontal layout option works for wider screens
- Existing functionality preserved (audio capture, transcription, RAG)
</success_criteria>

<output>
After completion, create `.planning/phases/03-resizable-layout/03-01-SUMMARY.md`
</output>
