---
phase: 05-dual-pane-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [gui.py]
autonomous: true

must_haves:
  truths:
    - "Main window shows two answer panes side by side"
    - "Detected question displayed at top of answer area"
    - "Left pane labeled 'Key Points'"
    - "Right pane labeled 'Script'"
    - "Layout responsive when window resized"
  artifacts:
    - path: "gui.py"
      provides: "Dual-pane answer layout"
      contains: "bullet_box"
    - path: "gui.py"
      provides: "Script pane"
      contains: "script_box"
  key_links:
    - from: "question_display"
      to: "answer_splitter"
      via: "QVBoxLayout stacking"
      pattern: "question.*splitter"
---

<objective>
Create dual-pane answer layout with question display at top.

Purpose: Enable displaying two answer formats (bullet points + conversational script) simultaneously for v2.1 milestone.
Output: Modified gui.py with restructured answer area showing question above two side-by-side answer panes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Current GUI implementation (key patterns to follow):
@gui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure answer area with question display at top</name>
  <files>gui.py</files>
  <action>
Modify AstraWindow._init_ui() to restructure the answer area:

1. Replace current answer_panel section with new structure:
   - Create `answer_area` QWidget as container
   - Add `question_display` QLabel at top showing detected question text
   - Add `answer_splitter` QSplitter(Horizontal) below question

2. The question_display should:
   - Use QLabel with word wrap
   - Styled to be visible but not dominant (smaller font than answers)
   - Show placeholder "Waiting for question..." initially

3. Keep existing content_splitter but reorganize:
   - Top: answer_area (question + dual panes)
   - Bottom: question_panel (transcription, last heard - keep as is)

Key points:
- Reuse existing styling patterns from the codebase
- Keep the content_splitter for Q/A section separation
- The answer_splitter is a NEW horizontal splitter inside answer_area
  </action>
  <verify>python -c "from gui import AstraWindow; print('Import OK')"</verify>
  <done>answer_area contains question_display QLabel above answer_splitter QSplitter</done>
</task>

<task type="auto">
  <name>Task 2: Create labeled bullet points and script panes</name>
  <files>gui.py</files>
  <action>
Add two answer panes to the answer_splitter:

1. Left pane (bullet points):
   - Container QWidget with QVBoxLayout
   - QLabel "Key Points" as header
   - QTextEdit `bullet_box` for bullet point answers
   - Placeholder: "• Key point 1\n• Key point 2\n• Key point 3"
   - Same styling as current answer_box

2. Right pane (script):
   - Container QWidget with QVBoxLayout
   - QLabel "Script" as header
   - QTextEdit `script_box` for conversational script
   - Placeholder: "Conversational script will appear here..."
   - Same styling as current answer_box

3. Configure answer_splitter:
   - Set sizes to 50%/50% split
   - setChildrenCollapsible(False)
   - Same handle styling as content_splitter

4. Add new signals to SignalBridge:
   - `bullet_token = pyqtSignal(str)` for streaming bullets
   - `script_token = pyqtSignal(str)` for streaming script
   - `question_update = pyqtSignal(str)` for question display

5. Connect new signals in _connect_signals():
   - bullet_token -> _on_bullet_token (append to bullet_box)
   - script_token -> _on_script_token (append to script_box)
   - question_update -> _on_question_update (update question_display)

6. Update existing answer flow:
   - Keep answer_box reference pointing to bullet_box for backward compatibility
   - The current _on_answer_token should write to bullet_box (Phase 6 will update this)

Note: Keep existing answer_box assignment for now (answer_box = bullet_box) so existing code paths continue to work until Phase 6 updates the generation logic.
  </action>
  <verify>python -c "from gui import AstraWindow, SignalBridge; s = SignalBridge(); print('bullet_token' in dir(s), 'script_token' in dir(s))"</verify>
  <done>Two labeled panes (bullet_box, script_box) in horizontal splitter, signals connected</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from gui import AstraWindow"` - imports without error
- [ ] `python gui.py --help` - runs without crash
- [ ] Verify AstraWindow has: question_display, bullet_box, script_box, answer_splitter
- [ ] SignalBridge has: bullet_token, script_token, question_update signals
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- GUI shows question display at top of answer area
- Two side-by-side answer panes with labels
- Layout resizes properly when window is resized
- Existing answer flow still works (backward compatible via answer_box alias)
</success_criteria>

<output>
After completion, create `.planning/phases/05-dual-pane-layout/05-01-SUMMARY.md`
</output>
