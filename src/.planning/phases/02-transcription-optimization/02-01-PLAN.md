---
phase: 02-transcription-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [config.py]
autonomous: true

must_haves:
  truths:
    - "Default Whisper model is tiny.en"
    - "Transcription uses beam_size=1"
    - "VAD filtering is enabled (vad_filter=True)"
    - "Transcription completes without errors"
  artifacts:
    - path: "config.py"
      provides: "Whisper model configuration"
      contains: 'WHISPER_MODEL = "tiny.en"'
  key_links:
    - from: "config.py WHISPER_MODEL"
      to: "transcriber.py get_whisper_model()"
      via: "import"
      pattern: "WHISPER_MODEL"
---

<objective>
Switch default Whisper model from base.en to tiny.en for faster transcription.

Purpose: Reduce transcription latency from ~2-3s to ~1s for 30s audio. tiny.en trades slight accuracy for significant speed improvement - acceptable for interview questions where keywords matter more than perfect transcription.

Output: Modified config.py with tiny.en as default model.

**Note:** TRANS-02 (beam_size=1) and TRANS-03 (vad_filter=True) are already implemented in transcriber.py from Phase 1 work. This plan only needs to change the model.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-memory-audio-pipeline/01-01-SUMMARY.md
@config.py
@transcriber.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change default Whisper model to tiny.en</name>
  <files>config.py</files>
  <action>
Change line 17 in config.py:

From:
```python
WHISPER_MODEL = "base.en"
```

To:
```python
WHISPER_MODEL = "tiny.en"
```

This is the ONLY change needed. Do NOT modify:
- transcriber.py (beam_size and vad_filter already set correctly)
- Any other config values
  </action>
  <verify>
Run: grep WHISPER_MODEL config.py
Expected: WHISPER_MODEL = "tiny.en"
  </verify>
  <done>config.py has WHISPER_MODEL = "tiny.en"</done>
</task>

<task type="auto">
  <name>Task 2: Verify transcription still works with tiny.en</name>
  <files>transcriber.py</files>
  <action>
Test that the tiny.en model loads and transcribes correctly.

Run a minimal test:
```python
import numpy as np
from transcriber import transcribe_audio

# Create 2 seconds of silence
silence = np.zeros(16000 * 2, dtype=np.int16)
result = transcribe_audio(silence)
print(f"Model loaded and transcription returned: '{result}'")
print("OK - tiny.en model works")
```

**What to verify:**
- Model loads without errors (first run downloads tiny.en if needed)
- Transcription returns empty string or minimal text for silence
- No exceptions thrown
  </action>
  <verify>python -c "import numpy as np; from transcriber import transcribe_audio; print(transcribe_audio(np.zeros(16000*2, dtype=np.int16))); print('OK')"</verify>
  <done>tiny.en model loads and transcribes without errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] config.py has WHISPER_MODEL = "tiny.en"
- [ ] transcriber.py still has beam_size=1 (unchanged)
- [ ] transcriber.py still has vad_filter=True (unchanged)
- [ ] Test transcription with tiny.en model works
</verification>

<success_criteria>

- config.py updated to use tiny.en model
- Transcription works with new model
- All verification checks pass
- Requirements TRANS-01, TRANS-02, TRANS-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-transcription-optimization/02-01-SUMMARY.md`
</output>
